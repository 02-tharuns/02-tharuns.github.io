# Copy to .env.production (gitignored) or, better, set VITE_API_BASE as a
# Vercel project environment variable — the deployed Render/Azure backend URL.
VITE_API_BASE=https://chappie-api.onrender.com
